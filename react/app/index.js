import React from 'react';
import ReactDOM from 'react-dom';
import { KanbanRedirect } from './kanban-redirect.js';

const kanbanRedirectNode = ReactDOM.render(<KanbanRedirect />, document.getElementById('kanban-redirect'));
